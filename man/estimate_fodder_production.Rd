% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/production.R
\name{estimate_fodder_production}
\alias{estimate_fodder_production}
\title{Estimate Fodder Production from CW, CY, and DS Data}
\usage{
estimate_fodder_production(
  cyd,
  dsd = NULL,
  cwd = NULL,
  allowable_use = 0.6,
  range_area_ha = 700,
  au_weight_kg = 60,
  grazing_months = 6,
  days_per_month = 30,
  daily_intake_fraction = 0.02,
  scale_kg_ha = 10,
  cyd_sheet = "cyd",
  dsd_sheet = "dsd",
  cwd_sheet = "re_cwd"
)
}
\arguments{
\item{cyd}{A CY calibration data frame, or an Excel workbook path.}

\item{dsd}{A DS calibration data frame. Leave as \code{NULL} when \code{cyd} is a
workbook path.}

\item{cwd}{A CW/CY/DS field data frame. Leave as \code{NULL} when \code{cyd} is a
workbook path.}

\item{allowable_use}{Allowable use fraction.}

\item{range_area_ha}{Available range area in hectares.}

\item{au_weight_kg}{Average animal-unit body weight in kilograms.}

\item{grazing_months}{Grazing period in months.}

\item{days_per_month}{Days assumed per grazing month.}

\item{daily_intake_fraction}{Fraction of body weight consumed per day.}

\item{scale_kg_ha}{Multiplier used to convert plot dry matter to kg/ha.}

\item{cyd_sheet}{Excel sheet name used when \code{cyd} is a workbook path.}

\item{dsd_sheet}{Excel sheet name used when \code{cyd} is a workbook path.}

\item{cwd_sheet}{Excel sheet name used when \code{cyd} is a workbook path.}
}
\value{
An object of class \code{r4c_production_result}.
}
\description{
Uses calibration sheets (\code{cyd}, \code{dsd}) and the \code{re_cwd} field sheet to
summarize dry fodder production for cutting-and-weighing (CW), comparative
yield (CY), and double sampling (DS). CW is calculated from observed \code{Ydk};
CY and DS are predicted from their calibration regressions.
}
\examples{
ex <- r4c_example_data()
estimate_fodder_production(ex$cyd, ex$dsd, ex$cwd)
}
