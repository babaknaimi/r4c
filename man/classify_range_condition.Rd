% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/range_condition.R
\name{classify_range_condition}
\alias{classify_range_condition}
\title{Classify Range Condition}
\usage{
classify_range_condition(
  x,
  palatability,
  sheets = NULL,
  merge_replicates = TRUE,
  label_col = NULL,
  special_rows = c(litter = "LFP", vigour = "SGP", canopy = "TCP", bare_soil = "BSP"),
  indicative_production = NULL,
  factor_weights = c(composition = 1, production = 1, canopy = 1, litter = 1, vigour = 1,
    soil_protection = 1),
  class_breaks = default_class_breaks(),
  aggregation_fun = mean,
  na.rm = TRUE
)
}
\arguments{
\item{x}{A data frame, a list of data frames, or an Excel workbook path.}

\item{palatability}{Palatability classes for the species rows. Supply either
a named vector or a two-column data frame mapping species to class.}

\item{sheets}{Optional sheet names when \code{x} is an Excel file.}

\item{merge_replicates}{Logical; if \code{TRUE}, replicated SF tables are merged
before classification.}

\item{label_col}{Optional label column index for SF tables.}

\item{special_rows}{Mapping of factor rows in the SF input.}

\item{indicative_production}{Optional indicative-state production reference.
If omitted, \code{TCP} is used as a proxy for present production.}

\item{factor_weights}{Named numeric vector of factor weights.}

\item{class_breaks}{Named numeric vector of lower score bounds.}

\item{aggregation_fun}{Summary function used across replicate columns or
within-row plot values.}

\item{na.rm}{Passed to the summary function.}
}
\value{
An object of class \code{r4c_condition_result}.
}
\description{
Calculates a six-factor range condition score from either:
}
\details{
\itemize{
\item a single SF data frame
\item a list of replicated SF data frames
\item an Excel workbook path
}

The function is deliberately transparent: all factor scores are returned,
and the default scoring assumptions can be overridden.
}
\examples{
ex <- r4c_example_data()

classify_range_condition(
  ex$sf_replicates,
  palatability = ex$palatability,
  indicative_production = 60
)
}
