% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sf_template.R
\name{classify_sf_template}
\alias{classify_sf_template}
\title{Classify Range Condition from an SF Template}
\usage{
classify_sf_template(
  x,
  score_profile = c("linear", "template_rank"),
  score_max = c(composition = 50, production = 15, canopy = 15, litter = 10, vigour = 5,
    soil_protection = 15),
  count_reference = c(litter = 5, vigour = 5),
  rank_fun = floor,
  class_breaks = default_class_breaks(),
  ...
)
}
\arguments{
\item{x}{A data frame in SF template format.}

\item{score_profile}{Either \code{"linear"} or \code{"template_rank"}.}

\item{score_max}{Maximum score for each six-factor component when
\code{score_profile = "template_rank"}.}

\item{count_reference}{Reference count used to rescale \code{litter} and \code{vigour}
rows when \code{score_profile = "template_rank"}.}

\item{rank_fun}{Function used to convert the unrounded template-rank sum to
the reported total rank. Defaults to \link{floor}.}

\item{class_breaks}{Named numeric vector of lower score bounds.}

\item{...}{Additional arguments passed to \code{\link[=prepare_sf_template]{prepare_sf_template()}} and
\code{\link[=classify_range_condition]{classify_range_condition()}}.}
}
\value{
An object of class \code{r4c_condition_result}.
}
\description{
This is a convenience wrapper for the field-template format used in the
example workbook. By default it uses the transparent linear scoring already
implemented by \code{\link[=classify_range_condition]{classify_range_condition()}}. Set \code{score_profile = "template_rank"} to apply a rank-style template profile that reproduces the
colleague-provided example output for the attached workbook while keeping the
profile parameters explicit and overridable.
}
